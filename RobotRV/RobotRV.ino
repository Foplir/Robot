#define byte motorA;
#define byte motorB;
#define byte sensorFA;
#define byte sensorFB;
#define byte sensorRA;
#define byte sensorRB;
#define byte sensorLA;
#define byte sensorLB;
#define byte sensorBA;
#define byte sensorBB;
#define byte sensorColor;

void setup() {
  pinMode(motorA, OUTPUT);
  pinMode(motorB, OUTPUT);
  pinMode(sensorFA, INPUT);
  pinMode(sensorFB, INPUT);
  pinMode(sensorRA, INPUT);
  pinMode(sensorRB, INPUT);
  pinMode(sensorLA, INPUT);
  pinMode(sensorLB, INPUT);
  pinMode(sensorBA, INPUT);
  pinMode(sensorBB, INPUT);
  pinMode(sensorColor, INPUT);
  
}

void loop() {
 
}
